{
  "name": "techcity",
  "scripts": {
    "css": "tailwindcss -i ./input.css -o ../static/site.css --minify",
    "tailwind": "tailwindcss -i ./input.css -o ../static/site.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}

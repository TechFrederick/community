{
  "scripts": {
    "css": "tailwindcss -i ./input.css -o out/site.css --minify",
    "tailwind": "tailwindcss -i ./input.css -o out/site.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
